{
  "name": "lun",
  "version": "1.4.0",
  "description": "Multi-agent consensus CLI — ask multiple AI agents and compare answers",
  "type": "module",
  "bin": {
    "lun": "./bin/lun.js"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check bin/lun.js && node --check server.js",
    "cli": "node bin/lun.js"
  },
  "keywords": ["ai", "cli", "multi-agent", "claude", "copilot", "kiro", "consensus"],
  "license": "MIT",
  "files": ["bin/", "src/", "README.md"],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "@fastify/websocket": "^11.0.2",
    "@fastify/static": "^8.1.0"
  }
}
